To remove:

date picker
date fns

To add:
Install Day.js: If you haven't already, you need to install Day.js in your project:

bash
Copy code
npm install dayjs
Format the Date Using Day.js:

javascript
Copy code
import dayjs from 'dayjs'; // Import dayjs

const handleDateChange = (date) => {
  setSelectedDate(date);
  console.log(date); // This logs the Day.js object

  // Format the date using Day.js
  const formattedDate = date.format('YYYY-MM-DD');
  console.log(formattedDate); // Logs the date in 'YYYY-MM-DD' format
};

Add a 10 second timeout for the spring apps because of rabbitmq.